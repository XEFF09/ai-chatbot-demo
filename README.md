# Set Up

- duplicate .env.example to .env
- set up hugging face api key from (https://huggingface.co/settings/tokens)

```
HUGGINGFACEHUB_API_TOKEN="hf_??"
```

- in this dir terminal type: docker-compose up -d
- wait until the containers are up and running, then open http://localhost:3030 in your browser
